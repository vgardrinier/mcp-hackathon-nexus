# API Key used by the MCP HTTP endpoint (choose any secret value)
API_KEY=choose-a-secret-api-key

# HTTP Server Port (for MCP HTTP endpoint)
HTTP_SERVER_PORT=3001

# Optional: Override the MCP server config path (defaults to ./servers/config.yml)
# MCP_SERVERS_CONFIG=./servers/config.yml

# Optional: Allow unauthenticated MCP access (for testing only - NOT recommended in production)
# ALLOW_UNAUTHENTICATED_MCP=false
